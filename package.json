{
  "name": "claude-talkback-mcp",
  "version": "0.1.0",
  "description": "MCP server that gives Claude Code a conversational voice. Speaks short, peer-style summaries aloud via Windows SAPI (works from WSL and native Windows; best-effort espeak/say elsewhere).",
  "type": "module",
  "bin": {
    "claude-talkback-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
